find_all_sources(. guilib_SRCS)
if(NOT TARGET_WIN32)
	list(REMOVE_ITEM guilib_SRCS ./GUIFontTTFDX.cpp)
endif()
list(REMOVE_ITEM guilib_SRCS ./GUIShader.cpp)
if(NOT TARGET_RPI)
    list(REMOVE_ITEM guilib_SRCS ./MatrixGLES.cpp)
endif()
add_sources(${guilib_SRCS})
